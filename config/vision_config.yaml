# 视觉参数配置文件（YAML格式）
# 修改后自动生效，无需重新编译

detection:
  confidence_threshold: 0.5
  nms_threshold: 0.25
  input_width: 640
  input_height: 640
  used_classes: 8       # 只使用前N类（-1=全部）

display:
  font_scale: 0.6
  font_thickness: 2
  line_thickness: 2
